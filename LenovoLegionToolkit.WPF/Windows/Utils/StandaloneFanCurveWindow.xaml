<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Utils.StandaloneFanCurveWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Resource.CustomFanCurve_Title}"
    Width="800"
    Height="600"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar Title="Fan Curve Test (Real Data)" Grid.Row="0" />

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ComboBox
                x:Name="_fanSelector"
                Grid.Row="0"
                MinWidth="400"
                Margin="0,0,0,20"
                HorizontalAlignment="Left"
                SelectionChanged="FanSelector_SelectionChanged" />

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="_fanControlStackPanel" />
            </ScrollViewer>
        </Grid>
    </Grid>
</local:BaseWindow>