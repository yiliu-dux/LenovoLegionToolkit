<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.SpecialKeyDetailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="480"
    Height="600"
    MinWidth="400"
    MinHeight="500"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <local:BaseWindow.Resources>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0,12,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,4" Panel.ZIndex="1" KeyboardNavigation.TabIndex="1" IsItemsHost="True" Background="Transparent" />
                            <ContentPresenter Grid.Row="1" ContentSource="SelectedContent" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,16,0" />
            <Setter Property="Padding" Value="0,0,0,6" />
            <Setter Property="Width" Value="Auto" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" HorizontalAlignment="Stretch">
                            <StackPanel HorizontalAlignment="Stretch">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                                <Rectangle x:Name="ActiveIndicator" Height="2" Fill="Transparent" RadiusX="1" RadiusY="1" Margin="0" HorizontalAlignment="Stretch" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                <Setter TargetName="ActiveIndicator" Property="Fill">
                                    <Setter.Value>
                                        <SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </local:BaseWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <TextBlock
                x:Name="_title"
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="12,0,16,0">
                <custom:CardControl Margin="0,0,0,12">
                    <custom:CardControl.Header>
                        <controls:CardHeaderControl Title="{x:Static resources:Resource.SpecialKeyDetail_Description}" />
                    </custom:CardControl.Header>
                    <TextBox
                        x:Name="_descriptionTextBox"
                        MinWidth="200"
                        TextChanged="DescriptionTextBox_TextChanged" />
                </custom:CardControl>

                <custom:CardControl Margin="0,0,0,12">
                    <custom:CardControl.Header>
                        <controls:CardHeaderControl x:Name="_modeHeader" Title="{x:Static resources:Resource.SpecialKeyDetail_Mode}" />
                    </custom:CardControl.Header>
                    <ComboBox
                        x:Name="_modeComboBox"
                        MinWidth="150"
                        SelectionChanged="ModeComboBox_SelectionChanged" />
                </custom:CardControl>

                <TabControl x:Name="_customPanel" Visibility="Collapsed">
                    <TabItem Header="{x:Static resources:Resource.Notification_SmartKeySinglePress}">
                        <StackPanel Margin="0,4,0,0" TextElement.FontWeight="Normal">
                            <TextBlock
                                Margin="0,0,0,8"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="{x:Static resources:Resource.SelectSpecialKeyPipelinesWindow_List_Description}"
                                TextWrapping="Wrap" />

                            <controls:LoadableControl
                                x:Name="_loaderSingle"
                                Margin="0,0,0,8"
                                IndicatorVerticalAlignment="Center">

                                <Border CornerRadius="8">
                                    <Border.Background>
                                        <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" />
                                    </Border.Background>

                                    <wpfui:DynamicScrollViewer MaxHeight="180">
                                        <ItemsControl
                                            x:Name="_listSingle"
                                            Margin="8,8,16,0"
                                            HorizontalContentAlignment="Stretch" />
                                    </wpfui:DynamicScrollViewer>
                                </Border>
                            </controls:LoadableControl>
                        </StackPanel>
                    </TabItem>

                    <TabItem Header="{x:Static resources:Resource.Notification_SmartKeyDoublePress}">
                        <StackPanel Margin="0,4,0,0" TextElement.FontWeight="Normal">
                            <TextBlock
                                Margin="0,0,0,8"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="{x:Static resources:Resource.SelectSpecialKeyPipelinesWindow_List_Description}"
                                TextWrapping="Wrap" />

                            <controls:LoadableControl
                                x:Name="_loaderDouble"
                                Margin="0,0,0,8"
                                IndicatorVerticalAlignment="Center">

                                <Border CornerRadius="8">
                                    <Border.Background>
                                        <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" />
                                    </Border.Background>

                                    <wpfui:DynamicScrollViewer MaxHeight="180">
                                        <ItemsControl
                                            x:Name="_listDouble"
                                            Margin="8,8,16,0"
                                            HorizontalContentAlignment="Stretch" />
                                    </wpfui:DynamicScrollViewer>
                                </Border>
                            </controls:LoadableControl>
                        </StackPanel>
                    </TabItem>
                </TabControl>

            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <wpfui:Button
                x:Name="_deleteButton"
                Grid.Column="0"
                MinWidth="100"
                Appearance="Danger"
                Click="DeleteButton_Click"
                Content="{x:Static resources:Resource.Delete}"
                Visibility="Collapsed" />

            <wpfui:Button
                Grid.Column="1"
                MinWidth="100"
                Appearance="Primary"
                Click="SaveButton_Click"
                Content="{x:Static resources:Resource.Save}" />

            <wpfui:Button
                Grid.Column="2"
                MinWidth="100"
                Margin="8,0,0,0"
                Click="CancelButton_Click"
                Content="{x:Static resources:Resource.Cancel}" />
        </Grid>
    </Grid>
</local:BaseWindow>
