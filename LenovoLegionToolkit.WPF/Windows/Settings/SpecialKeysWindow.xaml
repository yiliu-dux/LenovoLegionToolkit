<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.SpecialKeysWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="500"
    Height="550"
    MinWidth="400"
    MinHeight="400"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <TextBlock
                x:Name="_title"
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="12,4,16,12">
                <custom:CardControl
                    Margin="0,4,0,8"
                    Icon="Info24">
                    <custom:CardControl.Header>
                        <TextBlock Text="{x:Static resources:Resource.SpecialKeysWindow_Description}" TextWrapping="Wrap" />
                    </custom:CardControl.Header>
                    <wpfui:Button
                        x:Name="_showHiddenButton"
                        Appearance="Primary"
                        Icon="Eye24"
                        Visibility="Collapsed"
                        Click="ShowHiddenButton_Click" />
                </custom:CardControl>
                <ItemsControl x:Name="_keyList" />
                <StackPanel x:Name="_hiddenArea" Margin="0,8,0,0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</local:BaseWindow>
