<wpfui:UiWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.FloatingGadgets.Custom"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding Source={x:Static resources:Resource.FloatingGadget_Custom_Title}}"
    Width="350"
    Height="550"
    Background="Transparent"
    ExtendsContentIntoTitleBar="True"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <wpfui:UiWindow.Resources>
        <system:Double x:Key="PrimaryFontSize">14</system:Double>
        <system:Double x:Key="SecondaryFontSize">14</system:Double>
        <system:Double x:Key="TertiaryFontSize">12</system:Double>
        <system:Double x:Key="HeaderFontSize">18</system:Double>

        <Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
            <Setter Property="Margin" Value="0,6" />
            <Setter Property="FontSize" Value="{StaticResource PrimaryFontSize}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource HeaderFontSize}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Margin" Value="0,0,0,15" />
        </Style>

        <Style BasedOn="{StaticResource {x:Type GroupBox}}" TargetType="GroupBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1,1,1,1" />
            <Setter Property="Margin" Value="0,10,0,0" />
            <Setter Property="FontSize" Value="{StaticResource SecondaryFontSize}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        </Style>
    </wpfui:UiWindow.Resources>

    <Grid Background="{DynamicResource SystemFillColorSolidNeutralBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType=wpfui:UiWindow}}" Grid.Row="0" />

        <Grid Grid.Row="1" Margin="16,0,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="_itemsStackPanel" Margin="0,0,8,0" />
            </ScrollViewer>

            <Button
                Grid.Row="2"
                Margin="0,16,0,0"
                HorizontalAlignment="Stretch"
                Click="CloseButton_Click"
                Content="{Binding Source={x:Static resources:Resource.Close}}" />
        </Grid>
    </Grid>
</wpfui:UiWindow>